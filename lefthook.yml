pre-commit:
  jobs:
    - name: typecheck
      run: pnpm typecheck
    - name: format
      run: pnpm fix
      glob:
        - "**/*.{js,jsx,ts,tsx,css}"
      stage_fixed: true
    - name: lint
      run: pnpm lint
