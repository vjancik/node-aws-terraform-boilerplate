## Environment Rules
- Use `pnpm` instead of `npm`.
- This project uses the latest `node` LTS version.
- Don't run `pnpm approve-builds`. Instead stop and ask me to run it, so I can check what it's installing.